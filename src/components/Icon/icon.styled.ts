import styled from '@emotion/styled';

export const SVG = styled('svg')({
  color: 'inherit',
  display: 'inline-block',
  stroke: 'currentColor',
  fill: 'currentColor',
  height: '16px',
  width: '16px',
  resize: 'both',
});
